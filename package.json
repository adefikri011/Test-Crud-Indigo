{
  "name": "crud-jquery-indogo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i ./assets/css/tailwind.css -o ./assets/css/style.css --minify",
    "build:js": "esbuild assets/js/app.entry.js --bundle --minify --format=iife --target=es2018 --outfile=assets/js/app.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "tailwindcss": "^3.4.17"
  }
}
